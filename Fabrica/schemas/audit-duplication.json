{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://fabrica.local/schemas/audit-duplication.json",
  "title": "AuditDuplicationPayload",
  "type": "object",
  "required": ["summary", "findings"],
  "properties": {
    "summary": { "type": "string" },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "severity", "title", "summary", "recommendation"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "severity": {
            "type": "string",
            "enum": ["critical", "medium", "low"]
          },
          "title": { "type": "string" },
          "summary": { "type": "string" },
          "evidence": { "type": "array", "items": { "type": "string" } },
          "recommendation": { "type": "string" },
          "production_safety": {
            "type": "string",
            "enum": ["safe-incremental", "needs-flag", "high-risk"]
          }
        },
        "additionalProperties": false
      }
    },
    "duplicate_code_clusters": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["description", "locations"],
        "properties": {
          "description": { "type": "string" },
          "locations": { "type": "array", "items": { "type": "string" } }
        },
        "additionalProperties": false
      }
    },
    "duplicate_api_surface": { "type": "array", "items": { "type": "string" } },
    "duplicate_business_rules": { "type": "array", "items": { "type": "string" } }
  },
  "additionalProperties": false
}
